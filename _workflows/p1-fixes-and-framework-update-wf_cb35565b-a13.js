export const meta = {
  name: 'p1-fixes-and-framework-update',
  description: 'Phase A: P1修正三份对标文档(事实修正+主张降级+描述层拆分)。Phase B: ChatGPT-5.5裁决落地框架(§3.6→Drift Ledger, §4.3→三级逃生口, §13→headroom引用)',
  phases: [
    { title: 'Fix Docs', detail: '并行修正三份对标文档的P1问题' },
    { title: 'Update Framework', detail: 'ChatGPT-5.5裁决落地框架四节' },
    { title: 'Verify', detail: '交叉验证所有改动一致性' }
  ]
}

// ============================================================
// Phase 1: Parallel P1 fixes to all three documents
// ============================================================
phase('Fix Docs')

const [fixA, fixB, fixC] = await parallel([
  // Document A: Downgrade core claim + add noise ratio caveat
  () => agent(`
你是文档编辑者。请修正文档A的核心主张强度。

文件: ./CacheAligner与AI框架OPEN-1对标分析.md

需要修改的内容:

1. §4标题和定位: 将"对 OPEN-1 候选机制的修正建议"改为"对 OPEN-1 候选机制的启发式观察与假设"。在§4开头增加一段: "> **定位修正(P1-1, 2026-06-13)**: 本章内容从'修正建议'降级为'启发式观察'。确定性检测信号在OPEN-1场景中的有效性取决于噪声比——若90%+ hash变动为正常迭代进展，本方案信号价值趋近于零。三层架构应被视为待验证的假设，而非可立即采纳的建议。该假设需在下个真实项目中试跑验证后方可考虑纳入框架正文。"

2. §6标题: 将"建议：框架 §13 外部参考增补"改为"有条件的框架增补建议（待试跑验证）"。在表格前增加: "> 以下增补建议的前提是：试跑证明hash检测能有效过滤正常迭代噪声(信号价值>随机)。在满足该前提之前，不应纳入框架正文。"

3. §8结论: 在"最终"前增加一句: "以上主张建立在一个未经真实项目验证的类比假设上(headroom工程域→框架治理域)，应被视为方法论启发而非操作建议。"

4. 全文: 搜索所有"建议"二字，根据上下文决定是否改为"启发"或"假设"或保留(如§7交叉引用处的"建议审阅"保留)

只做这些修改，不改动文档结构和其他内容。修改后读回验证。
`, { phase: 'Fix Docs' }),

  // Document B: Fix facts + downgrade universality claim
  () => agent(`
你是文档编辑者。请修正文档B的事实错误和主张过度问题。

文件: ./SmartCrusher方法论提取.md

需要修改的内容:

1. §2.1 错误关键词数量: 全文搜索"30+"，将所有"30+关键词"改为"12个关键词(error, exception, failed, failure, critical, fatal, crash, panic, abort, timeout, denied, rejected)"。确保代码位置引用也检查——如果写了"error_keywords.rs:17-30"，确认这个引用是否正确(源码中ERROR_KEYWORDS长度为12)。

2. §5.1 标题和内容: 将"任何信息筛选场景的通用原则"改为"结构化信息筛选中的条件原则"。在§5.1开头增加: "> **主张修正(P1-2, 2026-06-13)**: 错误项绝对优先在SmartCrusher中成立的前提是：错误项有明显可穷举的文本标记(12个关键词字符串匹配)。迁移到其他场景时，需要满足同等前提——错误项必须(1)有明确的文本/结构标记(2)误保留的代价低于误丢弃的代价(3)不存在隐私/安全/对抗性顾虑。在不满足这些条件的场景中(如语义错误判断、安全审查、对抗性输入)，此原则不可直接迁移。"

3. §5.1"迁移到其他场景"段落: 在三个示例(金融异常交易/学术事实错误/代码安全漏洞)后增加: "> **跨领域验证状态**: 以上迁移示例未经真实场景验证，是基于结构类比的外推，应被视为假设而非已证实的通用原则。每个示例的前提条件需独立检验。"

4. §8.4 增加: 在"lossless_min_savings_ratio=30%是经验值"段末增加: "此外，有损路径中的CCR恢复假设LLM会主动调用检索工具——如果LLM未检索(CCR自身承认的失败模式)，有损压缩退化为不可逆信息丢失。"

5. 全文: 将"任何信息筛选场景"替换为"结构化信息筛选场景"，"通用原则"替换为"条件原则"(保留§5其他三个原则的类似降级)

只做这些修改。修改后读回验证。
`, { phase: 'Fix Docs' }),

  // Document C: Split descriptive/prescriptive + add ETF R5 kill-test
  () => agent(`
你是文档编辑者。请修正文档C的描述层与建议层混淆问题。

文件: ./CCR作为逃生口案例研究.md

需要修改的内容:

1. 在§4(分级逃生口设计模式)开头，在现有内容之前插入: "> **定位修正(P1-3, 2026-06-13)**: 本章的分级逃生口模式是对CCR设计决策的**描述性分析(descriptive)**——它描述了headroom如何在压缩场景中处理信息不完整性。将其应用于框架§4.3修订属于**独立的设计考量(prescriptive)**，需要独立论证。以下各节的'建议'标签仅指涉前者；后者见§5(给框架§4.3的具体建议)，且有独立的条件约束。软信息压缩恢复(CCR场景)与硬工具故障停止(框架§4.3场景)是两类不同的问题，不应共用同一升级链。"

2. 在§5(给框架§4.3的具体建议)末尾, §5"设计考虑"段落之前，增加一节: "### 5.5 Kill-test: ETF R5消融事故在引入L2后是否仍被阻止?\n\n> **测试场景**: 假设框架v1.3已纳入三级逃生口。复现ETF R5事故条件——bash不可用、文件无法创建、回测数据缺失。AI按新规则判断:\n>\n> 1. 检索路径是否可用? **否**——bash失效意味着所有工具调用(包括检索)均不可执行。→ L1不可用，升级。\n> 2. 是否可以降级继续? AI有上一轮回测的缓存结果。按L2规则——信息质量下降但非完全不可用——AI可能判断为'可用缓存数据标注staleness后继续'。\n> 3. 这是否正确? **错误**——缓存数据是完全不同的输入(上一版本的回测结果)，不是'同源但降级'的信息。使用它就是'用替代数据继续'。\n>\n> **结论**: L2在当前措辞下无法阻止ETF R5事故——因为它将'不同源替代'误分类为'同源降级'。L2必须增加硬条件: **替代数据必须与缺失数据来自同一源、同一查询、仅时效或精度降级**。不同源的数据=Level 3。\n>\n> **教训**: 此kill-test表明，L2的'降级继续'条件必须从'信息质量下降'收紧为'同源信息的时效/精度/覆盖度降级'。任何涉及数据源替换的场景直接升级到Level 3。此条件现已纳入§5建议措辞。"

3. §5建议1(L1)措辞中，在条件(c)后增加: "(e)检索返回的数据源必须与被压缩数据同源——不同源的数据视为Level 3触发条件"

4. §5建议2(L2)措辞中，在条件(a)后增加: "标注中必须区分'同源降级'(时效/精度/覆盖度下降)和'异源替换'(使用了不同来源的替代数据)。后者直接触发Level 3。"

5. §7"CCR是补充，不是替代"的场景对照表中，在最后增加一行: "缓存数据异源替换(如ETF R5: 用旧版回测结果替代当前缺失数据)" | "正确响应(停止)" | "危险(可能误判为Level 2降级继续)" | "§4.3 Level 3"

只做这些修改。修改后读回验证。
`, { phase: 'Fix Docs' })
])

// ============================================================
// Phase 2: Framework updates per ChatGPT-5.5 verdict
// ============================================================
phase('Update Framework')

const frameworkUpdate = await agent(`
你是框架编辑者。请按ChatGPT-5.5独立审查的裁决，更新AI协作项目全生命周期框架。

文件: ./AI协作项目全生命周期框架.md

ChatGPT-5.5裁决摘要:
- 文档A(条件采纳): detector-only + 三层分离，改名为 Loop Drift Ledger，新增观测不新增阻断
- 文档B(仅参考): 不纳入框架正文，§13引用标注为"可借鉴的架构原则"非"已验证通用方法论"
- 文档C(条件采纳): 纳入三级逃生口，Level 2限缩为"同源降级的可逆准备"
- 关键句: "新增观测，不新增阻断；新增 drift ledger，不新增常态人工审批"

需要做的修改(只做这些，不改动框架其他内容):

### 修改1: §3.6 将候选机制改为 Loop Drift Ledger

在§3.6的"候选机制"段落后，将原来的:
"候选机制（待验证，不默认启用）：在 Loop 中插入轻量级方向一致性自我报告..."
替换为:
"**候选机制（v1.3草案，待验证，不默认启用）：Loop Drift Ledger（循环漂移账本）**

基于 ChatGPT-5.5 独立审查（2026-06-13）对三份 headroom 对标分析文档的裁决，将候选机制从'方向一致性自我报告'升级为结构化漂移账本。核心原则：**新增观测，不新增阻断；新增 drift ledger，不新增常态人工审批。**

账本记录字段限定为**可验证的事实**（非 AI 主观判断）：
- 本轮新增/删除/放宽/收紧的约束（与上轮 Prompt/Spec 的逐字段 diff）
- 数据源、样本、阈值、评估口径的变更
- 输出 schema 或关键章节结构的变更
- 本轮做出的不可逆或难逆选择（如删除文件、覆盖数据、改变方向）

三层架构：
1. **事实层（Fact）**：确定性检测——Spec 锚点 hash + Prompt 参数 diff + 输出 schema hash。不依赖 AI 判断，纯计算，可 diff、可复核。
2. **解释层（Annotation）**：AI 对事实信号的语义标注——'本轮为什么动了这些参数'。必须与事实字段**分栏呈现**，标注为低信任。
3. **裁决层（Judgment）**：HG-1 时人类阅览 drift ledger，独立判断'这些变动在正确方向上吗？需要回退吗？'

**已知盲区（必须随账本一起报告）**：
- 未检测到结构变化 ≠ 未发生语义漂移
- 注意力偏移、证据标准下降、默认假设变化可能不留下结构化痕迹
- Drift ledger 覆盖的是'留下痕迹的漂移'，不是全部 OPEN-1

**与 ChatGPT-5.5 / DeepSeek-v4-pro 分歧点的调和**：
- DeepSeek-v4-pro 判定的'覆盖不足'：由事实层+解释层回应（新增观测信号）
- ChatGPT-5.5 判定的'闸门太多'：由裁决层回应（不新增阻断，人只在 HG-1 看汇总账本）
- 交集共识：新增观测，不新增阻断

**预登记验证指标**（与 §1.5 死亡判据共用基线）：
- 每个里程碑 drift ledger 捕获的实质变更数
- 人类判定为'有价值漂移信号'的比例
- 误报导致的审查耗时（警戒线：HG-1 审查耗时增加 >20%）
- 未被 ledger 捕获但事后发现的漂移案例
- AI 主观自评与客观变更账本的差异率
- 是否降低返工或方向回滚次数

若 2-3 个真实项目后 drift ledger 只产生噪音、未发现有价值问题，降级为可选。若多次提前发现方向偏移，升级为默认版机制。"

### 修改2: §4.3 增加分级逃生口

在§4.3的当前三条逃生口规则之后，在"### 4.4 Prompt 质量自检"之前，增加:

"**分级逃生口（v1.3草案，基于 CCR 案例研究 + ChatGPT-5.5 裁决）**

当前三条规则属于 Level 3（硬失效·停止报告）——这是 ETF R5 消融事故血的教训，不可削弱。以下新增 L1 和 L2 不替代 L3，而是处理不同层次的信息不完整性。

**Level 1：检索/验证恢复**
- 触发：信息可能不完整，但存在已知、可用、来源隔离、时效明确的检索或验证路径
- 动作：(a) 向执行者告知不完整的程度和检索路径 (b) 提供检索/验证界面 (c) 执行者自主判断是否需要更多信息
- 硬约束：检索返回的数据必须与被替代数据**同源**——不同源的数据直接触发 Level 3。检索轮次上限 3 轮。
- 适用实例：搜索结果被截断→提供完整查询参数；文件内容被轮次压缩→提供 headroom_retrieve hash；数据库采样返回→提供分页/导出端点

**Level 2：同源降级继续（限缩版）**
- 触发：信息质量下降但来自**同一数据源**——仅时效、精度或覆盖度下降，数据源未替换
- 动作：(a) 标注不确定性来源、程度和影响范围 (b) 仅继续执行**不依赖该缺失信息的可逆准备工作**（草稿、候选清单、待验证假设、下一步计划）(c) **不得**用于最终判断、外部提交、高风险结论
- 硬红线：凡关键结论依赖缺失信息，必须停止或升级到人类裁决。不同数据源替换（如用旧版回测结果替代当前缺失数据）= Level 3，绝不走此路径。
- 适用实例：实时数据源不可用→用最近缓存标注 staleness(同源)；API 返回部分字段失败→用可用字段标注缺失维度(同源)

**Level 3：停止报告（保留为最高硬安全层）**
- 保留当前三条规则不变
- 增加升级关系说明：Level 3 是最终兜底——L1 检索路径不可用 且 L2 降级不安全/不可标注/异源替换 时到达此层级

**分级逃生口升级规则**：
L1(检索恢复) → L2(同源降级) → L3(停止报告)
- 从低层向高层升级需要明确理由
- L3 触发的硬条件：检索路径不可用 | 数据源被替换(异源) | 不确定性不可标注 | 降级信息已导致错误 | 工具/数据完全不可用
- 条件恢复后允许高层降回低层继续执行

**Kill-test(ETF R5 事故复现)**: bash不可用+文件无法创建+回测数据缺失。L1检索路径不可用→升级。AI有上一轮缓存结果——但这是**异源数据**(不同版本的输入)，不是同源降级→L2不适用→直接L3停止报告。通过。"

### 修改3: §13 外部参考增加 headroom 条目

在§13.2"值得关注的外部工作"表格中(或在表格后适当位置)，新增:

"| **headroom 三组件 (CacheAligner / SmartCrusher / CCR)** | (1) detector-only 哲学(检测与处置解耦, §3.6 Loop Drift Ledger 的架构原则来源) (2) 多信号优先级排序(约束>锚点>相关性, Workflow 模式选择的参照) (3) 可逆压缩与分级恢复(§4.3 分级逃生口的方法论启发) | 本框架可借鉴的是**架构原则**而非具体机制——headroom 操作在确定性字节层(错误=关键词匹配, 检测=hash比较)，本框架操作在语义治理层。三项方法论启发均标注为'可借鉴的架构原则'，未经跨域验证，不等同于'已验证的通用方法论'。详见配套文档《CacheAligner与AI框架OPEN-1对标分析》《SmartCrusher方法论提取》《CCR作为逃生口案例研究》及《ChatGPT-5.5独立审查_headroom对标三文档》。 |"

### 修改4: §1.6 OPEN-1 状态更新

在§1.6待决项表中，OPEN-1行后增加:
"| **OPEN-1 处置方案** | v1.3草案: Loop Drift Ledger(事实层+解释层+裁决层) + 预登记6项验证指标。ChatGPT-5.5独立裁决(2026-06-13): 条件采纳，关键约束为'只覆盖留下可观察痕迹的漂移' + '不新增阻断，只新增观测'。DeepSeek-v4-pro(作者)与ChatGPT-5.5(独立)的方向性分歧已调和——交集='新增观测不新增阻断'。仍待人类裁决+真实项目试跑。 | 中→高(有独立裁决后佐证增强) | 待试跑验证 | §3.6 + ChatGPT-5.5独立审查报告 |"

只做以上四处修改。不要改动框架其他内容。修改后读回§3.6和§4.3验证改动正确。
`, { phase: 'Update Framework' })

// ============================================================
// Phase 3: Verify all changes are consistent
// ============================================================
phase('Verify')

const verification = await parallel([
  () => agent(`
验证文档A、B、C的P1修正是否一致且完整。

请检查:
1. 文档B: "30+"是否已全部替换为"12个关键词"? "任何信息筛选场景"是否已全部改为"结构化信息筛选场景"?
2. 文档A: §4标题是否已改为"启发式观察与假设"? §6是否增加了前提条件?
3. 文档C: §4是否增加了descriptive/prescriptive区分? ETF R5 kill-test是否正确?(bash不可用→L1不可用→缓存数据是异源→不适用L2→L3)
4. 三份文档的交叉引用是否仍然有效?(P0-1和P0-2的修改不应被P1修改覆盖)
`, { phase: 'Verify' }),

  () => agent(`
验证框架修改是否正确且不破坏现有结构。

文件: ./AI协作项目全生命周期框架.md

请检查:
1. §3.6: "Loop Drift Ledger"是否正确插入? 三层架构(事实/解释/裁决)是否清晰? 盲区声明是否存在?
2. §4.3: 分级逃生口是否正确插入在当前三条规则之后、"### 4.4"之前? ETF R5 kill-test是否在L2描述中?
3. §13: headroom条目是否加入了外部参考表? 措辞是否为"可借鉴的架构原则"非"已验证通用方法论"?
4. §1.6: OPEN-1行是否增加了裁决状态更新?
5. 框架目录(## 目录)是否需要更新以反映新增内容?
6. 框架的其他章节(§5 L2 Loop, §6 L3 Workflow, §7 L4 Retrospect, §14变更记录)是否被意外改动?
`, { phase: 'Verify' })
])

return {
  phase1_fixes: { docA: !!fixA, docB: !!fixB, docC: !!fixC },
  phase2_framework: !!frameworkUpdate,
  phase3_verification: verification.filter(Boolean).map(v => v?.substring?.(0, 500) || 'verified')
}
