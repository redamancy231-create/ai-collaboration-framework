# Codex CLI 独立审查任务：两项冻结期变更复核

## 你的角色

你是 **ChatGPT-5.5 (via Codex CLI)**，独立审查者。你的后端（GPT-5）和上下文环境（Codex CLI）与本次变更的执行者（DeepSeek-v4-pro, via Claude Code CLI）**完全隔离**。

## 审查任务

对 2026-06-15 在《AI协作项目全生命周期框架》v1.5.1 冻结期执行的两项变更进行独立复核：

1. **OPEN-5 关闭**：新增 §8.8「项目规模分档」
2. **目录重组**：76 个文件平铺→按功能分入 4 个子目录

---

## 变更 1：§8.8 项目规模分档

### 背景

OPEN-5 登记的问题：Closure（§8）缺项目规模分档，当前闭合检查清单和终期产物要求是"一刀切"的——半天探索和正式研究执行同一套流程。

### 新增内容位置

文件：`<PROJECT_ROOT>\AI协作项目全生命周期框架.md`

**§8.8 项目规模分档**（插在 §8.7 和 §9 之间），包含：
- 三档定义表：S（半天探索）/ M（一周小项目）/ L（正式研究）
- 三档闭合要求对照表（11 行 × 4 列：闭合要求 / S / M / L）
- 档位升级规则（实际投入超上限→下次 HG-1 自动升级）
- 分档与项目类型的正交说明

**同步更新**：
- §8.4 检查清单头注：标注当前清单为 L 档完整版
- §1.6 OPEN-5 状态："待补"→"已覆盖 → §8.8"

### 审查要点

1. **冻结期合规**：此项自称"诚实性产物"（补分档不增加新机制，只区分已有机制的适用深度）。是否准确？
2. **分档定义合理性**：S/M/L 的投入上限（≤4h/≤1周/无上限）和 Loop 轮数上限（≤3/≤20/无上限）是否与框架其他部分（§4.2 收敛条件、§1.4 使用强度分档）一致？
3. **闭合要求对照表**：S 档做减法是否合理？有没有 S 档不该跳过但被跳过的（如"跨项目方法论写回"对于有重要教训的半天探索）？
4. **档位升级规则**：是否可操作？（"在下次 HG-1 自动升级"——HG-1 如何检测实际投入超上限？）
5. **正交关系**：项目类型×项目规模的正交框架是否正确？有没有类型和规模的隐含耦合被忽略了？
6. **与框架其他部分的交叉引用**：是否需要同步更新 §1.4（使用强度分档，与 S/M/L 有什么关系？）、§7.4（Retrospect 轻量版/深度版判定是否引用规模分档？）

---

## 变更 2：目录重组

### 背景

原 `<PROJECT_ROOT>\` 目录包含 76 个文件平铺，混合了核心框架、治理工具、审查报告、提示词、研究材料、协议包、脚本等。

### 重组方案

```
根目录 (11 文件)：
  AI协作项目全生命周期框架.{md,json,docx}     ← 核心
  methodological-review-sop.{md,json}         ← 治理
  meta-audit-checklist.{md,json}              ← 治理
  框架级成熟度评估表.{md,json}                 ← 治理（v1.5.1 新增）
  v1.5.1冻结期_待执行协议清单.md                ← 治理（v1.5.1 新增）
  md_to_docx_framework.py                     ← 工具

_reviews/ (47 文件)：全部审查报告 + 提示词 + 残留 txt
_research/ (17 文件)：headroom 对标 + 版本草案 + 弃用工具
_protocols-and-tools/ (4 文件)：协议包
_archive/ (4 文件)：旧版归档（未动）
```

### 审查要点

1. **根目录保留的文件是否合理**：`md_to_docx_framework.py` 是否应该留在根目录？`v1.5.1冻结期_待执行协议清单.md` 是否应该归入 `_protocols-and-tools/`？
2. **子目录划分逻辑**：`_reviews` 和 `_research` 的边界是否清晰？有没有文件放错了？
3. **遗漏的文件**：原来 76 个文件的完整清单是否都归入了某个子目录或保留在根目录？有没有遗漏的？
4. **内部引用**：重组后主框架中的文件引用（如 `_research/框架v1.5.1_新增节草案.md`）是否全部更新正确？
5. **OPEN-4 试读者体验**：一个第一次打开此目录的人能否快速找到核心文件？

---

## 输出格式

```
## 审查结论（摘要）

## 变更 1 审查：§8.8 项目规模分档
### 逐点评价
### 问题与建议

## 变更 2 审查：目录重组
### 逐点评价
### 问题与建议

## 综合建议
```
