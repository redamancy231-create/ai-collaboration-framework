# 独立审查任务：AI协作项目全生命周期框架 v1.5.1 三件套同步质量

## 你的身份

你是 ChatGPT-5.5 (GPT-5)，通过 Codex CLI 运行。你与被审对象的作者（DeepSeek-v4-pro）**后端不同、上下文隔离**——你不共享作者的 memory 或 CLAUDE.md。你的审查应保持独立和坦诚。

## 审查对象

以下三个文件位于工作目录：

1. `AI协作项目全生命周期框架.md` — 框架主文档，约 2600 行，版本 v1.5.1
2. `AI协作项目全生命周期框架.json` — 框架结构化 JSON，约 2100 行，**刚刚从 v1.5 同步到 v1.5.1**
3. `AI协作项目全生命周期框架.docx` — 框架 Word 文档，由 pandoc 从 md 自动生成

## 背景（供你理解上下文，不是审查结论）

- 框架 v1.5.1 新增了四个节：§3.7.0（事件流健康度监测）、§3.7.4.1（自适应权重淘汰）、§9.7（经验注入上下文预算规则）、§9.8（研究经验对象 REO）
- 方法论来源：Evolver 项目分析（arXiv:2604.15097）
- 今天（2026-06-14）刚完成了 JSON 从 v1.5→v1.5.1 的同步和 DOCX 的重新生成
- md 的变更记录（§14）和文末状态行也刚更新

## 审查维度与权重

### D1: md/JSON 跨件一致性（30%）
- JSON 中记录的结构是否与 md 中的实际内容一致？
- 版本号、日期、状态描述在两个文件中是否吻合？
- JSON 中 `changelog.v1_5_1` 是否准确反映了 md §14 的内容？
- JSON 中新增的结构节点（event_stream_health / adaptive_weight_retirement / experience_injection_budget / research_experience_objects）是否与 md 正文对应？
- `derived_from`、`independent_reviews`、`provenance_notes` 在 JSON 中是否完整？

### D2: v1.5→v1.5.1 同步完整性（25%）
- md 头部版本行、§14 变更记录标题、文末状态行是否全都更新到了 v1.5.1（没有残留 v1.5）？
- md 目录是否更新（变更记录条目从 v1.5→v1.5.1）？
- JSON 的 `metadata.version` 是否为 "1.5.1"？
- 两个文件中是否还有任何引用"v1.5"但应该是"v1.5.1"的地方？

### D3: 内容质量与内部矛盾（20%）
- v1.5.1 新增的四节在 md 中是否自洽（例如：§3.7.0 声称"只观测不阻断"与 §3.7.1 原则一致吗？§3.7.4.1 的权重数值是否与草案文件一致？）
- 证据等级标注是否一致——四个新增节都标注为 [Sp]，md 正文、JSON、和变更记录中的标注是否统一？
- 审查记录（`independent_reviews`）中的评分/日期/裁决是否自洽？
- md 中的交叉引用（如"详见草案 §X.Y"）是否存在断链？

### D4: JSON 结构质量（15%）
- JSON 是否有效、可解析？
- 新增节点的键名是否与已有命名惯例一致？
- 嵌套深度是否合理？是否有重复或遗漏的字段？

### D5: DOCX 生成质量（10%）
- 如果你能读取 docx 内容（通过 python-docx 等方式），检查：
  - 是否只有一个目录（而非两个）？
  - 关键 v1.5.1 内容（§3.7.0, §3.7.4.1, §9.7, §9.8）是否存在于正文？
- 如果不能读取二进制 docx，请诚实说明并跳过此维度（不要用 md 推测 docx 质量）

## 输出要求

1. **综合评分**（0-10，保留一位小数）
2. **按维度分项评分**（D1-D5，各 0-10）
3. **发现清单**（每条标注严重级别：P0 阻断 / P1 高 / P2 中 / P3 低）
4. **修正建议**（每条发现对应具体修改方案）
5. **总体裁决**：READY（无需修改）/ MINOR（小幅修改后可用）/ MAJOR（需重要修改）/ REJECT（不可用）

请将审查报告写入：`codex_review_v1.5.1_三件套同步审查.md`
并生成 JSON 版本：`codex_review_v1.5.1_三件套同步审查.json`

报告开头须声明你的实际模型名称和后端。
